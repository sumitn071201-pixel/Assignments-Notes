//Design Java Application to Accept strings from the user and store it in a arraylist. Find all strings which start with A, store them in list2(Note use filter function to find the strings) write all strings from list2 in a file data.txt, one string on each line


package com.demo;

import java.util.Iterator;
import java.util.Scanner;

public class AcceptStrings {

	public static void main(String args) {
		
		System.out.println("Enter a String: ");
		
		Scanner sc = new Scanner(System.in);
		for (int i =0; i<= length.arrlst;i++) {
			type type = (type) .next();
			
		} 
				
				
		String[] arrlst = new String[args.length() + 1];
		System.arraycopy(args, 0, arrlst, 0, args.length());
		arrlst[args.length()] = args;
		
		
	}

}
